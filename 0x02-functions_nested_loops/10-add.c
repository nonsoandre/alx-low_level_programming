#include "main.h"
#include <stdio.h>
/**
 * add - adds two numbers
 * @a: first parameter to be added
 * @b: second parameter to be added
 * Return: sum of params
 */

int add(int a, int b)
{
		int sum;

		sum = a + b;
		return (sum);
}
